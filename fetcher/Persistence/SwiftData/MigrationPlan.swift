import Foundation
import SwiftData

enum SchemaMigrationPlan {
    /// Placeholder for future SwiftData versioned schemas.
    /// Automatic migration covers additive MVP changes; introduce
    /// VersionedSchema types here when breaking changes are required.
}
